spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lbgconnect?useSSL=false&serverTimezone=UTC
    username: root
    password: MonkeyDREeiZLeBest669
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  sql:
    init:
      mode: always

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

app:
  storage:
    upload-dir: uploads
